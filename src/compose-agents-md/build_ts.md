## Build and Test Commands

Check package.json for typeCheck, lint and test commands, make sure all three pass for the whole repo.
Start with type check, then lint:fix, finally tests.
`lint:fix` auto-fixes many issues (import order, formatting, etc.) — prefer it over `lint`
